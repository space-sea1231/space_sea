#include <bits/stdc++.h>
#define int long long
using namespace std;
int a, b;
signed main(){
    ios::sync_with_stdio(0);
    cin.tie();
    cin >> a >> b;
    printf("%lld\n", (a-1)*(b-1)-1);

    return 0;
}