#include <iostream>
#include <stdio.h>
#define __Debug

using namespace std;
typedef long long ll;

int n;

signed main() {
  cin.tie(nullptr) -> ios::sync_with_stdio(false);
  /*Input*/
  cin >> n;
  if (n <= 15) {
    using namespace Pts1;
    
  }
  /*Init*/
  /*Solve*/
  /*Output*/
  return 0;
}