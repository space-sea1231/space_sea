#include <iostream>
#include <stdio.h>
#include <cstring>
#define __Debug

using namespace std;
typedef long long ll;
int a[10];
signed main() {
    cin.tie(nullptr) -> ios::sync_with_stdio(false);
    memset(a, -0x3f, sizeof(a));
    printf("%d\n", a[0]);
    return 0;
}