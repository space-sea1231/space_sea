struct Discretization{
    int num, len;
    int idx[N<<1];
    void Add(int x){
        idx[++num]=x;
    }
    void Get(){
        sort(idx+1, idx+num+1);
        len=unique(idx+1, idx+num+1)-idx-1;
    }
};