#include <bits/stdc++.h>
using namespace std;
const int N=1e5+10;
int a[N];
signed main(){
    // freopen("tmp.in", "r", stdin);
    // freopen("ans.out", "w", stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    memset(a, 0, sizeof(a));
    printf("%d", a[1]);

    return 0;
}