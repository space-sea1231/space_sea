#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("ge.in", "r", stdin);
    freopen("ge.out", "w", stdout);
    cin >> t;
    while (t--) {
        cout << 0 << '\n';
    }
	return 0;
}