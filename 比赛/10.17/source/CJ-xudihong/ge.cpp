#include <bits/stdc++.h>
using namespace std;
struct M
{
    int to, w, c;
};
vector<M>e[20];
int n, m;
signed main()
{
    freopen("ge.in", "r", stdin);
    freopen("ge.out", "w", stdout);
    ios :: sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int T;
    cin >> T;
    while (T--)
    {
        cin >> n >> m;
        puts("NIE");
    }
    return 0;
}